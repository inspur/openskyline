modules/文件夹
放置封装好的Vue components. 这点同components类似
与components文件夹不同的是,
componenets里面是系统级别的公共组件, 原则上和业务关系不大，是脱离于业务抽象出来的组件
modules里是业务级别的组件，并且按业务线做了更细致的划分，每个业务线独立的业务组件放在各自的目录里，彼此之前不会有冲突
如：cloud-monitor文件夹里面是云监控子系统提取出来的公共业务组件。
目的是抽象和复用代码，方便各个业务模块调用
